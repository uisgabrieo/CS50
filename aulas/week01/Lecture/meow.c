#include <stdio.h>
#include <cs50.h>

int meow(int n);

int main(void)
{
    // int contador = 3;
    // while(contador != 0)
    // {
    //     printf("Contador = %i\n", contador);
    //     contador--;
    // }

    printf("%i", meow(10));
}

int meow(int n)
{
    return n;
}

